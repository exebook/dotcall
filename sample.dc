: foo(f) {
	setTimeout(:() { f(true) }, 200)
}

: bar(x, f) { setTimeout(:() { f(500 + x) }, 200) }

: main() {
	if (foo.()) {
		var d = bar.(55)
		log(d)// outputs 555
	}
}
main()

A = [1,2,3]
each i A log(A[i])
//A.forEach(:i log(i); )

//if (x) { a = 0; break }
//if (x) a = 0^